import FormComponent from "./FormComponent";

const Form = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <FormComponent />
    </div>
  );
};

export default Form;
